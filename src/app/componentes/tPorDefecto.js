import { crearT } from '../i18n/index.js';

/**
 * Traductor por defecto para los componentes que no reciben uno. La app le
 * pasa el suyo; esto solo evita que un olvido deje la pantalla en blanco.
 */
export const tPorDefecto = crearT('es');
