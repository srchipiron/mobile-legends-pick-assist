name: Publicar la app

on:
  push:
    branches: [main]
  workflow_run:                      # republica cuando la ingesta trae datos nuevos
    workflows: ["Actualizar datos meta", "Partidas profesionales"]
    types: [completed]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    # Tope del JOB entero: los pasos con red ya lo tenían, pero las pruebas,
    # la compilación o la subida podían colgar el runner seis horas.
    timeout-minutes: 30
    # Un bot que descarto su corrida (comparar-ingesta en rojo) tambien
    # "completa": sin esto se desplegaba igual, y si los datos del repo ya
    # pasaban de 24 h, con veinte minutos de ingesta para nada.
    if: ${{ github.event_name != 'workflow_run' || github.event.workflow_run.conclusion == 'success' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: '20'
      # node_modules ENTERO, por hash del lockfile. La cache de npm de
      # setup-node solo guarda las descargas: medido, npm ci seguia en 3,7-4,7
      # minutos con ella (despliegue 99, vigilancia 77). Con node_modules
      # guardado, el paso se salta. npm ci BORRA node_modules antes de
      # instalar, asi que solo corre si la cache no ha traido nada.
      - id: modulos
        uses: actions/cache@v4
        with:
          path: node_modules
          key: modulos-${{ runner.os }}-node20-${{ hashFiles('package-lock.json') }}

      # npm ci, no npm install: instala EXACTAMENTE lo que dice el lockfile.
      # Con npm install, una version menor mala de una dependencia entraba sola
      # en el despliegue, y eso es indepurable desde el movil.
      - if: steps.modulos.outputs.cache-hit != 'true'
        run: npm ci

      # ¿Hacen falta datos nuevos? El bot de datos ya descarga dos veces al dia
      # y los commitea, y un winrate de ventana de 7 dias no se mueve en horas.
      # Volver a descargar en CADA push de codigo costaba diez minutos por
      # despliegue y, peor, con cancel-in-progress cada push reiniciaba la
      # descarga del anterior desde cero: cinco commits seguidos dejaron la app
      # publicada 25 minutos por detras. Se descarga solo si el repositorio
      # lleva mas de 24 horas sin datos nuevos, o sea, si el bot lleva un dia
      # fallando.
      #
      # 24 h y no 12: el bot programa dos corridas al dia, pero GitHub ejecuta
      # el cron con 3-5 horas de retraso (medido en sus seis corridas: cron a
      # las 05:17 y 17:17, arranques reales a las 10:28, 19:50, 11:35, 21:53,
      # 10:05 y 19:56). Los datos del repositorio tienen normalmente entre 12 y
      # 17 horas, asi que con 12 se descargaba casi siempre y no se ahorraba
      # nada. 24 sigue tres veces por debajo del limite de rancio (72 h).
      - name: Frescura de los datos del repositorio
        id: frescura
        run: |
          node -e "const d=require('./public/data/roam-meta.json');
          const h=(Date.now()-new Date(d.generatedAt))/3.6e6;
          console.log('datos del repositorio de hace', h.toFixed(1), 'h');
          require('fs').appendFileSync(process.env.GITHUB_OUTPUT, 'fresca=' + (h < 24) + '\n');"

      - name: Ingesta
        if: steps.frescura.outputs.fresca != 'true'
        # A un temporal: hasta saber que esta corrida no es peor que la
        # guardada, los datos buenos no se tocan.
        #
        # Y con continue-on-error, que NO es lo que habia antes: antes el paso
        # escribia encima de public/data, asi que dejarlo fallar publicaba la
        # app nueva con datos congelados sin ninguna senal. Ahora escribe a un
        # temporal y quien decide es el paso de abajo, mirando lo que hay.
        id: ingesta
        continue-on-error: true
        # Tope de tiempo, porque "fallar" ya lo cubre continue-on-error pero
        # "colgarse" no: la ingesta hace ~570 peticiones con 15 s de timeout
        # cada una, asi que con la API caida a medias puede tardar 140 minutos
        # y el despliegue se queda ahi aunque ya sepa publicar con los datos del
        # repositorio. Lo normal medido son 9-10 minutos; 20 deja margen doble.
        # Un timeout cuenta como fallo, y el paso de abajo hace lo de siempre.
        timeout-minutes: 20
        # Las imagenes tambien a temporales: una corrida descartada dejaba sus
        # iconos y retratos en public/ y salian en dist/ sin haber pasado el filtro.
        run: node scripts/ingest.mjs --rank glory --days 7 --out /tmp/nueva.json --iconos /tmp/objetos --retratos /tmp/heroes

      - name: Elegir con qué datos se publica
        # Tres casos:
        #   la corrida nueva es buena     -> se usa
        #   es peor, o la API esta caida  -> se usan los datos del repositorio,
        #                                    que pasaron este mismo filtro el dia
        #                                    que se commitearon
        #   ademas estan rancios          -> se para el despliegue
        #
        # Antes, con la API caida no se podia publicar NINGUN cambio de codigo
        # aunque el repositorio tuviera datos perfectos. Paso de verdad: el
        # despliegue de 1.6.0 se quedo sin publicar por un UPSTREAM_REQUEST_FAILED
        # que no tenia nada que ver con el codigo que se subia.
        run: |
          if [ "${{ steps.ingesta.outcome }}" = "skipped" ]; then
            echo "Datos del repositorio recientes: ingesta omitida, se publica con ellos."
          elif [ "${{ steps.ingesta.outcome }}" = "success" ] \
             && node scripts/comparar-ingesta.mjs /tmp/nueva.json public/data/roam-meta.json; then
            cp /tmp/nueva.json public/data/roam-meta.json
            mkdir -p public/objetos public/heroes
            [ -d /tmp/objetos ] && cp -n /tmp/objetos/*.png public/objetos/ 2>/dev/null || true
            [ -d /tmp/heroes ] && cp -n /tmp/heroes/*.jpg public/heroes/ 2>/dev/null || true
            echo "Se publica con la corrida nueva."
          else
            echo "::warning::La ingesta ha fallado o resuelve menos; se publica con los datos del repositorio."
          fi

      - name: Comprobar que los datos valen para publicar
        # El limite de horas es lo que sustituye a "regenerado hace menos de
        # media hora". Un winrate de una ventana de 7 dias no se mueve en un
        # dia; a los tres ya es otra cosa, y publicar eso seria mentir.
        run: |
          node -e "const d=require('./public/data/roam-meta.json');
          const h=(Date.now()-new Date(d.generatedAt))/3.6e6;
          const cruces=Object.values(d.counters??{}).reduce((n,f)=>n+Object.keys(f??{}).length,0);
          console.log('datos generados hace', h.toFixed(1), 'h ·', d.heroCount ?? 0, 'heroes ·', cruces, 'cruces');
          if (h > 72) { console.error('Los datos tienen mas de 72 horas: no se publica'); process.exit(1); }
          if (!(d.heroCount ?? 0)) { console.error('Sin estadisticas'); process.exit(1); }
          if (cruces < 1000) { console.error('Sin matriz de counters'); process.exit(1); }"

      # UNA definicion de «las pruebas»: la de package.json. Antes el
      # despliegue llamaba a las cuatro a mano y npm test (PR y vigilancia)
      # podia perder una sin que nadie lo notara. Hay prueba de que npm test
      # sigue siendo las cuatro.
      - name: Pruebas (npm test)
        run: npm test

      - name: Compilar
        env:
          BASE_PATH: /${{ github.event.repository.name }}/
        run: npm run build

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    timeout-minutes: 15
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
